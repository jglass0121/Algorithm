import java.util.*;
class Solution {
    public int solution(int[] array) {
        int answer = 0;
        Arrays.sort(array);
        int mid = array.length/2;
        int res = array[mid];
        return res;
    }
}